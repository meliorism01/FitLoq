# API base URL — swap this once the backend is live
VITE_API_BASE_URL=https://api.fitloq.com/v1

# Feature flags (frontend-only, for demo purposes)
VITE_ENABLE_MOCK_DATA=true
VITE_ENABLE_AI_COACH=true
VITE_ENABLE_FOOD_SCANNER=true
